# Наша работа: роли, приоритеты и общий запуск

Обновлено: 23 сентября 2026.
Ветка: [`codex/scoring-rules`](https://github.com/BAITC-Hacks/hack-6d0b8f9a-dofp/tree/codex/scoring-rules).
Правила `roles-v1.2`, снимок `moneygraph.snapshot.v1`, поддержанный C.
Граф `3651a714` включён без изменений; полный расчёт с CSV проверен на 2 248 клиентах.

## Простыми словами

Участник графа считает показатели каждого клиента: от скольких людей поступили
деньги, скольким он отправил, какие суммы и связи видны в данных.

Наша часть принимает готовые показатели и отвечает на три вопроса:

1. Какую роль клиент предположительно выполняет: собирает, распределяет,
   передаёт деньги дальше, является возможным концом цепочки или координатором?
2. Кого аналитику полезнее проверить сначала?
3. На каких конкретных числах основано это предположение и чего мы не знаем?

Пример для понимания: узел получает от многих разных отправителей. При выполнении
остальных условий правило может назвать его кандидатом в сборщики. Код сохраняет
количество плательщиков, условия правила и ограничения данных. Это не доказательство
нарушения и не решение о блокировке счёта.

## Что готово

| Работа | Состояние |
|---|---|
| Шесть ролей, включая peripheral | Реализованы правила с явными порогами |
| Оценка поддержки роли | Реализована; это не вероятность виновности |
| Приоритет проверки | Реализован; четыре числовых вклада доступны отдельно |
| Короткое и подробное объяснения | Реализованы; короткое поле <=200 символов |
| Три CSV по ТЗ | Сохраняются все роли, кластеры с гипотезами и первые 30 клиентов |
| Изоляты, seed, четвёртое колено, самопереводы | Добавлены ограничения и проверки |
| Адаптер входа из таблицы участника A | Принимает готовые записи DataFrame, Timestamp/NaT и предупреждения |
| Тесты | 112 тестов A+B+C прошли; проверены настоящий API и --serve |
| Совместимость с графом A | Полный FeatureTable принят; роли и top-list рассчитаны |
| CLI и снимок | Одна команда; SHA-256, версии и сохранение предыдущего результата при сбое |
| API C | Совместимость с опубликованным a749d9c; три HTTP-экспорта совпадают побайтово |

Мы используем готовые функции графа и не пишем API/интерфейс. Пакетный расчёт
работает одной командой из нашей ветки; --serve подключает опубликованный API C.
Координация и интеграция веток закреплены за C. Его исходники не меняем и в нашу
ветку не переносим; способ запуска через второй checkout описан в README.

```bash
python -m pip install .
python -m moneygraph run --data ./data --out ./out
```

Нужен Python 3.12 и три выданных Parquet в data. Полная установка и Windows-команды
есть в [README](../README.md). Выходная папка печатается в терминале.

## Какие файлы смотреть

| Файл | Назначение |
|---|---|
| [roles.py](../moneygraph/analytics/roles.py) | Входные признаки, адаптер, правила шести ролей |
| [ranking.py](../moneygraph/analytics/ranking.py) | Приоритеты, очередь, записи для экспорта |
| [explanations.py](../moneygraph/analytics/explanations.py) | Человеческие объяснения с числами |
| [test_scoring.py](../tests/scoring/test_scoring.py) | Тесты поведения и пограничных случаев |
| [validate_features.py](../tests/scoring/validate_features.py) | Проверка готового набора признаков и чувствительности порогов |
| [scoring.md](scoring.md) | Точный контракт для коллег, формулы и запуск |
| [scoring-decisions.md](scoring-decisions.md) | Обоснования решений и их ограничения |
| [pipeline.md](pipeline.md) | Формат для API, публикация и причины архитектурных решений |
| [scoring-sensitivity.md](scoring-sensitivity.md) | Численная проверка порогов и спорного выбора support/caps |

## Как открыть в VS Code на Linux

Выполняй команды по очереди. Этот вариант создаёт отдельную папку для нашей ветки.
Git и VS Code должны быть установлены, а GitHub-аккаунт — иметь доступ к приватному
репозиторию. Если папка `~/moneygraph-scoring` уже создана, повторное клонирование
не нужно: переходи к разделу обновления.

```bash
cd ~
git clone --branch codex/scoring-rules https://github.com/BAITC-Hacks/hack-6d0b8f9a-dofp.git moneygraph-scoring
cd moneygraph-scoring
code .
```

`code .` открывает текущую папку. Если команда code не найдена, открой VS Code
вручную и выбери File → Open Folder → moneygraph-scoring в домашней папке.
При ошибке доступа нужно авторизовать Git на своём компьютере; подключение GitHub
в чате автоматически не авторизует терминал ноутбука. Токены в чат не отправляй.

Открой `docs/scoring-progress.md`; предпросмотр Markdown — Ctrl+Shift+V.

## Как видеть новые изменения

После сообщения, что новый коммит отправлен в нашу ветку, в терминале проекта:

```bash
cd ~/moneygraph-scoring
git status --short --branch
git pull --ff-only
git log -5 --oneline
```

Проверь, что status показывает `codex/scoring-rules`. Pull получает опубликованные
коммиты; VS Code затем показывает обновлённые файлы. Это не трансляция набора текста:
неотправленные изменения из рабочей среды ассистента на ноутбуке не появляются.

Если Git сообщает о конфликте, расходящихся ветках или локальных изменениях,
остановись и передай текст сообщения. Не используй reset --hard или force push
для решения такой ситуации. `--ff-only` не создаёт неожиданный merge-коммит.

Последний коммит и затронутые им файлы:

```bash
git show --stat HEAD
```

Посмотреть ветки команды без их изменения:

```bash
git fetch origin
git branch -r
```

## Как проверить нашу часть

В терминале из корня проекта с Python 3.12:

```bash
python3 --version
python3 -B -m unittest discover -s tests/scoring -v
```

Без MONEYGRAPH_DATA_DIR выполняются 25 тестов scoring, интеграционная группа
пропускается. Для базовых тестов не нужны pandas, графовые библиотеки, данные или
API-ключи. Полный прогон 28 тестов описан в scoring.md. Успешный результат
заканчивается `OK`. Это тесты, не запуск интерфейса.

## Что делать дальше

1. **Готово:** связка опубликованного графового модуля A и нашего scoring проверена
   на реальных файлах. Все gid сохранены; предупреждения перенесены, исходный
   FeatureTable не изменён; очередь и записи для экспорта согласованы.
2. **Готово по назначению пользователя:** общий запуск, запись трёх CSV и снимок
   с проверенными хешами. Контракт для API описан в pipeline.md.
3. **Готово на стороне B:** снимок принимается настоящим API C; --serve, карточки,
   вклады, ограничения и точность экспортов проверены.
4. **Команда:** общий прогон с выгрузками и интерфейсом, поиск произвольного gid,
   репетиция демонстрации. Объединение веток — по командной договорённости.

Ближайшая задача C — принять экран в браузере и объединить ветки. Отдельное
решение тимлида — оставляем ли support в приоритете: замена на величину с caps
меняет 13/20 лидеров. Текущая формула не изменена, ограничения передаются в UI.

## Что проверено в репозитории

На момент этого обновления:

- main — `9511d9b`, только исходный README;
- наша ветка — код `0763d79` и последующие коммиты документации/интеграционных тестов;
- codex/data-graph — `3651a714`, опубликованы загрузчик, валидация, граф,
  метрики, сообщества, тесты и документация;
- обновление A `e951658` добавляет документацию/тесты устойчивости; production
  модули побайтово совпадают с 3651a714. Их повторный перенос в B не требуется;
- feat/data-graph — `9511d9b`;
- codex/api-ui — опубликована, `a749d9c`; контракт и тесты приняты для интеграции.

Совместный прогон выполнен: включён коммит A 3651a714 с его исходниками,
тестами и документацией; Git blob SHA проверены. Тесты вызывают функции коллеги,
не содержат своих графовых алгоритмов. Проверено 2 248 клиентов, 105 кластеров,
19 изолятов, 444 узла границы; сохранены 97 повторяющихся транзакций.

Прежнее измерение до результата scoring — 0.827 с; scoring отдельно — 0.104 с.
Теперь полный CLI с экспортом и публикацией занимает 2–3 секунды.
API и установка зависимостей в этот замер не входят. Общий pytest A+B+C с запуском
HTTP-сервера и открытием всех 2 248 карточек в новой версии C занимает около
32 секунд; это другой замер, не время расчёта.
Это измерение среды разработки, не гарантия для ноутбука жюри.

Роли: consolidator 103, distributor 107, transit 42, terminal 148,
coordinator 18, peripheral 1830. Это гипотезы правил, не размеченная истина.
Данные и готовые списки клиентов в GitHub не загружены.

Перед каждой доработкой проверяем актуальные ветки/файлы, читаем контракты и
переиспользуем готовую работу коллег. Данные и секреты не добавляем в коммиты.

## Справка по командам

- VS Code CLI: https://code.visualstudio.com/docs/configure/command-line
- Git pull: https://git-scm.com/docs/git-pull
